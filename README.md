# AdventureTourBooking

## Running with Docker

Build and start the application together with PostgreSQL using Docker Compose:

```bash
docker-compose up --build
```

Once the containers are healthy, the API is available at `http://localhost:8080` and connects to the PostgreSQL service defined in the compose file.
